<?xml version="1.0"?>
<odoo>
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">estate</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_estate_property_available': True}</field>
    </record>


    <record id="estate_property_view_tree" model="ir.ui.view">
        <field name="name">estate_property_tree</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <tree string="Tmp" decoration-success="state in ['offer_received','offer_accepted']" decoration-bf="state == 'offer_accepted'" decoration-muted="state =='sold'">
                <field name="name"/>
                <field name="state"/>
                <field name="property_type_id"/>
                <field name="postcode"/>
                <field name="tags_ids"/>
                <field name="bedrooms"/>
                <field name="living_area"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="date_availability" optional="hide"/>
                <field name="best_price"/>
            </tree>
        </field>
    </record>

    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">estate_property_form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Form">
                <header>
                    <header>
                        <button name="sold_action" type="object" string="SOLD" invisible="state in ['canceled', 'sold']"/>
                        <button name="cancel_action" type="object" string="CANCEL" invisible="state in ['canceled', 'sold']"/>

                    </header>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <div>
                        <h1>
                            <field name="name"/>
                        </h1>
                        <h4>
                            <field name="tags_ids" widget="many2many_tags"  options="{'color_field': 'color'}"/>
                        </h4>
                    </div>
                    <group>
                        <group>
                            <field name="property_type_id" options="{'no_create': true}"/>
                            <field name="postcode"/>
                            <field name="date_availability"/>
                        </group>
                        <group>
                            <field name="expected_price"/>
                            <field name="selling_price"/>
                            <field name="best_price"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>

                                <field name="living_area"/>
                                <field name="facades"/>
                                <field name="garage"/>
                                <field name="garden"/>
                                <field name="garden_area" invisible="not garden"/>
                                <field name="garden_orientation" invisible="not garden"/>

                                <field name="total_area"/>

                            </group>
                        </page>
                        <page string="Other Info">
                            <group>
                                <field name="buyer_id"/>
                                <field name="sales_person_id"/>
                            </group>
                        </page>
                        <page string="Offers">
                            <group>
                                <field name="offer_ids" readonly="state in ['offer_accepted', 'sold', 'canceled']"/>

                            </group>
                        </page>

                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="estate_property_search" model="ir.ui.view">
        <field name="name">estate_property_search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Tests">
                <field name="name"/>
                <field name="property_type_id"/>
                <field name="postcode"/>
                <field name="expected_price"/>
                <field name="bedrooms"/>
                <field name="living_area" filter_domain="[('living_area','>=', self)]"/>
                <field name="facades"/>
                <filter string="DateAvailable" name="estate_property_date_available"
                        domain="[('date_availability', '&lt;', context_today().strftime('%Y-%m-%d'))]"/>
                <filter string="Available" name="estate_property_available"
                        domain="[('state', 'in', ['new','offer_received'])]"/>
                <filter string="Post Code" name="estate_property_post_cpde_grouping"
                        context="{'group_by': 'postcode'}"/>
            </search>
        </field>
    </record>
</odoo>